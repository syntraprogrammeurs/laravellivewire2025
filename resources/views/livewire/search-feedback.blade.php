<div class="text-lg text-white/90">
    @if($currentSearch)
        Je zoekt naar: <span class="font-mono font-bold text-white">{{ $currentSearch }}</span>
    @else
        Typ iets in het zoekveld hierboven...
    @endif
</div> 